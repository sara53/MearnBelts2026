/**
 * appendChild
 * ---------------------------
 * == accept only node dataype
 * == accept one child
 * == return element child
 * -------------------------------
 * append
 * ---------------------------
 * == accept  node dataype | string datatype
 * == accept more than one  child
 * == return undefined
 * -------------------------------
 */
// var parent = document.getElementById("parent");
// var h1 = parent.firstElementChild;

// var text = document.createTextNode("Hello angular group");

// var element = document.createElement("p"); // as a node
// var span = document.createElement("span"); // as a node

// span.innerHTML = "Hello Span";
// element.append("Hello angular group");
// element.append(text);

// element.setAttribute("class", "jsClass");

// // dom -- change - style

// function execute() {
// 	h1.remove();
// 	// h1.after(element);
// 	// h1.before(element);
// 	// parent.append(element);
// }
